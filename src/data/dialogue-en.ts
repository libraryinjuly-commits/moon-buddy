import type { DialoguePack } from "@/data/dialogue.types";
import { enLocale } from "@/lib/i18n/locales/en";

export const dialogueEn: DialoguePack = {
  welcomeGuide: enLocale.welcomeGuide,
  welcomeSpeech: enLocale.welcomeSpeech,
  thankSpeech: enLocale.thankSpeech,
  dialogues: enLocale.dialogues,
};
