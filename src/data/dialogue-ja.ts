import type { DialoguePack } from "@/data/dialogue.types";
import { jaLocale } from "@/lib/i18n/locales/ja";

export const dialogueJa: DialoguePack = {
  welcomeGuide: jaLocale.welcomeGuide,
  welcomeSpeech: jaLocale.welcomeSpeech,
  thankSpeech: jaLocale.thankSpeech,
  dialogues: jaLocale.dialogues,
};
